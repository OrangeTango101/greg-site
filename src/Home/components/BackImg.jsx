export default function BackImg(props) {

    <img 
        src={props.src} 
        style={{
            height: props.height,
            top: props.top,
            left: props.left
        }}
    />

}